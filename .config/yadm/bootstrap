#!/usr/bin/env bash
# ============================================================================
# ⚙️ YADM Bootstrap Script
# ============================================================================
# This script bootstraps the user's environment by installing necessary
# tools and configurations using Homebrew and other utilities.
# It is intended to be run as part of the YADM dotfiles management system.
# ============================================================================

set -euo pipefail

# ============================================================================
# COLORS
# ============================================================================

readonly RED='\033[0;31m'
readonly GREEN='\033[0;32m'
readonly YELLOW='\033[1;33m'
readonly BLUE='\033[0;34m'
readonly MAGENTA='\033[0;35m'
readonly CYAN='\033[0;36m'
readonly BOLD='\033[1m'
readonly RESET='\033[0m'

# ============================================================================
# GLOBALS
# ============================================================================

readonly LOG_FILE="$HOME/.config/yadm/bootstrap.log"
readonly TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S')

OS=""
ARCH=""
IS_WSL=false
K3SUP_TLS_SAN="127.0.0.1"
K3S_SETUP_STATUS="not_available"

# ============================================================================
# LOGGING
# ============================================================================

log() { echo "[${TIMESTAMP}] $1" >> "$LOG_FILE"; }

header() {
    echo -e "\n${BOLD}${CYAN}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${RESET}"
    echo -e "${BOLD}${CYAN}$1${RESET}"
    echo -e "${BOLD}${CYAN}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${RESET}\n"
    log "=== $1 ==="
}

success() { echo -e "${GREEN}✅ $1${RESET}"; log "SUCCESS: $1"; }
info()    { echo -e "${BLUE}ℹ️ $1${RESET}"; log "INFO: $1"; }
warn()    { echo -e "${YELLOW}⚠️ $1${RESET}"; log "WARNING: $1"; }
error()   { echo -e "${RED}❌ $1${RESET}"; log "ERROR: $1"; }
step()    { echo -e "${MAGENTA}🔧 $1${RESET}"; log "STEP: $1"; }

prompt() {
    local msg="$1"
    local default="${2:-N}"
    local response
    read -rp "$(echo -e "${CYAN}${msg} (Y/N) [Default: $default]: ${RESET}")" response
    echo "${response:-$default}"
}

cleanup() {
    local code=$?
    if [ $code -ne 0 ]; then
        error "Bootstrap failed with exit code $code"
        info "Check log: $LOG_FILE"
    fi
}
trap cleanup EXIT

show_completion_summary() {
    print_header "🎉 Bootstrap Completed Successfully"

    echo -e "${GREEN}All tasks finished without critical errors.${RESET}"
    echo -e ""

    # Kubernetes status summary
    if [ "${K3S_SETUP_STATUS:-}" = "installed" ]; then
        echo -e "${CYAN}☸️  Kubernetes cluster:${RESET}"
        echo -e "  ${GREEN}✓${RESET} Local k3s cluster is ready"
        echo -e "  ${BLUE}•${RESET} Use ${YELLOW}kubectl --context=local${RESET} to interact with your cluster"
        echo -e "  ${BLUE}•${RESET} Or switch context: ${YELLOW}kubectx local${RESET}"

    elif [ "${K3S_SETUP_STATUS:-}" = "installed_pending" ]; then
        echo -e "${CYAN}☸️  Kubernetes cluster:${RESET}"
        echo -e "  ${YELLOW}⏳${RESET} Cluster installed but readiness check pending"
        echo -e "  ${BLUE}•${RESET} Try manually: ${YELLOW}kubectl --context=local get nodes${RESET}"

    elif [ "${K3S_SETUP_STATUS:-}" = "skipped" ]; then
        echo -e "${CYAN}☸️  Kubernetes cluster setup (optional):${RESET}"
        echo -e "  ${BLUE}•${RESET} To install later: ${YELLOW}k3sup install --local --local-path ~/.kube/config --merge --context local${RESET}"

    elif [ "${K3S_SETUP_STATUS:-}" = "failed" ]; then
        echo -e "${CYAN}☸️  Kubernetes cluster:${RESET}"
        echo -e "  ${RED}✗${RESET} Cluster installation failed"
        echo -e "  ${BLUE}•${RESET} Check logs: ${YELLOW}$LOG_FILE${RESET}"
        echo -e "  ${BLUE}•${RESET} Try again: ${YELLOW}k3sup install --local --local-path ~/.kube/config --merge --context local${RESET}"

    elif [ "${K3S_SETUP_STATUS:-}" = "not_available" ]; then
        echo -e "${CYAN}☸️  Kubernetes cluster:${RESET}"
        echo -e "  ${BLUE}•${RESET} Use Orbstack to spin up a local k8s cluster: ${YELLOW}open the app and select Kubernetes${RESET}"
    fi

    echo -e ""
    echo -e "${CYAN}📝 Next steps:${RESET}"
    echo -e "  ${BLUE}*${RESET} Restart your shell in order for all changes to take effect"
    echo -e "  ${BLUE}*${RESET} Type ${YELLOW}help${RESET} to see available commands (if aliases are configured)"

    if [ "${IS_WSL:-false}" = true ] && [ "${K3S_SETUP_STATUS:-}" != "failed" ]; then
        echo -e ""
        echo -e "${CYAN}🖥️  Interacting with the Kubernetes cluster installed inside WSL on Windows Apps (Lens, etc.):${RESET}"
        echo -e "  ${BLUE}•${RESET} Create/edit Windows Kubeconfig: ${YELLOW}mnt/c/Users/%USERNAME%/.kube/config${RESET}"
        echo -e "  ${BLUE}•${RESET} The cluster is accessible from Windows via WSL IP"
        echo -e "  ${BLUE}•${RESET} Update the cluster's server IP to $K3SUP_TLS_SAN${RESET}"
    fi

    echo -e "\nFull log available at: ${CYAN}$LOG_FILE${RESET}"
}

# ============================================================================
# STEP 1 — OS CHECK
# ============================================================================

check_os() {
    step "Verifying operating system..."

    OS=$(uname -s)
    ARCH=$(uname -m)

    info "Operating System: $OS"
    info "Architecture: $ARCH"

    if [[ "$OS" != "Darwin" && "$OS" != "Linux" ]]; then
        error "Unsupported OS: $OS"
        exit 1
    fi

    if [[ "$OS" = "Linux" ]] && grep -qi 'microsoft' /proc/version 2>/dev/null; then
        IS_WSL=true
        K3SUP_TLS_SAN=$(ip -4 addr show eth0 | grep -oP '(?<=inet\s)\d+(\.\d+){3}' | head -n1)
        info "WSL environment detected"
    fi
}

# ============================================================================
# STEP 2 — BREWFILE
# ============================================================================

install_brew_deps() {
    step "Selecting Brewfile..."

    local brewfile

    if [ "$OS" = "Darwin" ]; then
        brewfile="$HOME/.config/yadm/Brewfile.macos"
    else
        brewfile="$HOME/.config/yadm/Brewfile.linux"
    fi

    info "Using Brewfile: $brewfile"

    if [[ ! -f "$brewfile" ]]; then
        error "Brewfile not found!"
        exit 1
    fi

    info "Installing Brewfile packages…"
    if brew bundle --file="$brewfile" --no-upgrade 2>&1 | tee -a "$LOG_FILE"; then
        success "Brewfile packages installed"
    else
        warn "Some Brew packages failed"
    fi
}

# ============================================================================
# STEP 3 — ZSH SETUP
# ============================================================================

setup_zsh() {
    step "Configuring zsh shell…"

    local current=$(basename "$SHELL" || echo "unknown")
    info "Current shell: $current"

    if [[ "$current" != "zsh" ]]; then
        warn "zsh is not default shell"

        local zsh_path
        zsh_path=$(command -v zsh)

        info "zsh path: $zsh_path"

        if ! grep -q "^${zsh_path}$" /etc/shells 2>/dev/null; then
            warn "Adding zsh to /etc/shells"
            echo "$zsh_path" | sudo tee -a /etc/shells >/dev/null
        fi

        info "Changing default shell…"
        if chsh -s "$zsh_path"; then
            success "Default shell changed to zsh"
        else
            error "Failed to change default shell"
            exit 1
        fi
    else
        success "zsh is already default"
    fi

    if [[ ! -f "$HOME/.zshrc" ]]; then
        error "~/.zshrc not found — is your dotfiles setup incomplete?"
        exit 1
    fi

    success "zsh config OK"
}

# ============================================================================
# STEP 4 — KUBECONFIG SETUP
# ============================================================================

setup_kube_config() {
    [[ "$OS" = "Darwin" ]] && return 0

    step "Setting up Kubernetes configuration directory…"

    mkdir -p "$HOME/.kube"

    local file="$HOME/.kube/config"

    if [[ -f "$file" ]]; then
        local backup="$file.backup.$(date +%Y%m%d-%H%M%S)"
        info "Backing up existing kubeconfig → $backup"
        cp "$file" "$backup"
    else
        info "Creating default kubeconfig"
        cat > "$file" << 'EOF'
apiVersion: v1
kind: Config
clusters: []
contexts: []
current-context: ""
preferences: {}
users: []
EOF
        chmod 600 "$file"
    fi

    success "Kube config initialized"
}

# ============================================================================
# STEP 5 — K3S INSTALL
# ============================================================================

setup_k3s() {
    [[ "$OS" = "Darwin" ]] && return 0
    command -v k3sup >/dev/null || return 0

    step "Local Kubernetes cluster setup (optional)"

    if [[ $(prompt "Install local k3s cluster?" "Y") =~ ^[Yy]$ ]]; then
        info "Installing k3s…"

        if k3sup install \
            --local \
            --local-path "$HOME/.kube/config" \
            --merge \
            --context "local" \
            --tls-san "$K3SUP_TLS_SAN"; then

            success "k3s installed"

            info "Checking cluster readiness…"

            if k3sup ready --context="local" --kubeconfig="$HOME/.kube/config"; then
                success "Cluster ready!"
                K3S_SETUP_STATUS="installed"
            else
                warn "Cluster readiness timed out"
                K3S_SETUP_STATUS="installed_pending"
            fi
        else
            error "k3s install failed"
            K3S_SETUP_STATUS="failed"
        fi
    else
        info "Skipping k3s installation"
        K3S_SETUP_STATUS="skipped"
    fi
}

# ============================================================================
# STEP 6 — SECRET DECRYPTION
# ============================================================================

decrypt_secrets() {
    if [[ $(prompt "Decrypt yadm secrets?" "Y") =~ ^[Yy]$ ]]; then
        step "Decrypting secrets…"

        if yadm decrypt; then
            success "Secrets decrypted"

            local repo=$(yadm remote get-url origin 2>/dev/null || echo "")

            if [[ "$repo" =~ ^https:// ]]; then
                if [[ $(prompt "Switch yadm to SSH remote?" "Y") =~ ^[Yy]$ ]]; then
                    local suggested=$(echo "$repo" | sed -E 's|https://([^/]+)/|git@\1:|')
                    read -rp "SSH URL (enter for suggested): " ssh
                    ssh="${ssh:-$suggested}"
                    yadm remote set-url origin "$ssh"
                    success "Remote updated to SSH"
                fi
            fi

        else
            warn "Secret decryption failed"
        fi
    else
        info "Skipping secret decryption"
    fi
}

# ============================================================================
# MAIN
# ============================================================================

main() {
    header "🚀 YADM Bootstrap Starting"

    echo "=== Bootstrap started at ${TIMESTAMP} ===" > "$LOG_FILE"

    check_os
    install_brew_deps
    setup_zsh
    setup_kube_config
    setup_k3s
    decrypt_secrets

    show_completion_summary
}

main "$@"
